<%= form_for(@book) do |f| %>
  <% if @book.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@book.errors.count, "error") %> prohibited this book from being saved:</h2>

      <ul>
      <% @book.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.number_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :book_title %><br>
    <%= f.text_field :book_title %>
  </div>
  <div class="field">
    <%= f.label :title_page_image_url %><br>
    <%= f.text_field :title_page_image_url %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
